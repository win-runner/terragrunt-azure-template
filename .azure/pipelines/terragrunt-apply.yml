trigger:
  branches:
    include:
      - main

pool:
  name: alz-mgmt
  vmImage: "ubuntu-latest"

variables:
  - template: templates/config.yaml

stages:
  - template: templates/terragrunt-template.yaml
    parameters:
      stageName: Apply
      action: apply
